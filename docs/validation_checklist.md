# Validation Checklist

1. **Spirit Name Present?**
   - Ensure that each spirit entry has a valid and present name.

2. **Rank Optional or Required?**
   - Confirm if rank is optional (can be null) or required (must have a value).

3. **Legion Count Type?**
   - Verify that legion count is either an integer or a string, not any other type.

4. **Raw Quote Format?**
   - Check if raw_quote includes folio/page and line range in the format "folio:page;line".

5. **Producer Name Presence?**
   - Ensure each spirit entry has a producer name present.

6. **Legion Count Type?**
   - Confirm that legion count is either an integer or a string, not any other type.

7. **Raw Quote Format?**
   - Verify if raw_quote includes folio/page and line range in the format "folio:page;line".

8. **Spirit Description Presence?**
   - Ensure each spirit entry has a valid and present description.

9. **Rank Optional or Required?**
   - Confirm rank is optional (can be null) or required (must have a value).

10. **Legion Count Type?**
    - Verify that legion count is either an integer or a string, not any other type.
    
11. **Raw Quote Format?**
    - Check if raw_quote includes folio/page and line range in the format "folio:page;line".

12. **Producer Name Presence?**
    - Ensure each spirit entry has a producer name present.

13. **Rank Optional or Required?**
    - Confirm rank is optional (can be null) or required (must have a value).

14. **Legion Count Type?**
    - Verify that legion count is either an integer or a string, not any other type.
    
15. **Raw Quote Format?**
    - Ensure raw_quote includes folio/page and line range in the format "folio:page;line".
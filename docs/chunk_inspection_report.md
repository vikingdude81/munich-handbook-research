# Chunk Inspection Report

## Inspection Results:

1. **chunk_05**: Contains `raw_quote` and `chunk_id` — all required fields present.
2. **chunk_17**: Missing `raw_quote` — gap in required field.
3. **chunk_34**: Contains `raw_quote` and `chunk_id` — all required fields present.

## Gaps Identified:

- `chunk_17` lacks `raw_quote`.